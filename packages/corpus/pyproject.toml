[project]
name = "exegia-corpus"
version = "0.1.0"
description = "Exegia Context-Fabric corpus manager"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.18,<0.10"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
module-name = "corpus"
